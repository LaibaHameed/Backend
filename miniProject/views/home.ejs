<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Home</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body>
    <div class="main w-full min-h-screen bg-zinc-900">
        <div class="flex gap-3 w-full justify-end px-4">
            <a href="/register" class="bg-green-500 hover:bg-green-700 text-sm py-2 px-10 text-white mt-2 rounded-md inline-block">Register</a>
            <a href="/login" class="bg-blue-500 hover:bg-blue-700 text-sm py-2 px-10 text-white mt-2 rounded-md inline-block">Login</a>
            <a href="/profile" class="bg-zinc-700 hover:bg-zinc-500 text-sm py-2 px-10 text-white mt-2 rounded-md inline-block self-end">Profile</a>
        </div>
        <div class="form px-10 text-zinc-200 w-full">
            <h1 class="text-3xl tracking-tight font-bold mb-2">All Posts</h1>
            <div class="posts py-10 flex flex-col-reverse items-center gap-3 flex-wrap">
                <% if(posts.length > 0){ %>
                    <% posts.forEach((post) => { %>
                        <div class="post w-2/4 px-3 py-4 rounded-md bg-zinc-800 border-[1px] border-zinc-700 flex flex-col gap-1">
                            <h2 class="font-bold text-md text-zinc-300">@<%= post.user.username %></h2>
                            <p class="text-zinc-200 text-lg tracking-tight self-center px-5 pt-1"><%= post.content %></p>
                            <p class="text-zinc-500 text-md tracking-tighter my-2 pt-1">
                                <%= post.formattedDate %> at <%= post.formattedTime %>
                            </p>
                            <div class="w-full flex gap-3 justify-end content-center items-center pr-3">
                                <p class="w-full"><%= post.likes.length %> likes</p>
                                <% if (user) { %>
                                    <a href="/like/<%= post._id %>" class="bg-blue-600 hover:bg-blue-700 text-white py-2 px-4 rounded-md">
                                        <%= post.likes.indexOf(user._id) === -1 ? "Like" : "Unlike" %>
                                    </a>
                                <% } else { %>
                                    <a href="/login" class="text-red-500 w-2/5 cursor-pointer">Login to like posts</a>
                                    <a href="" class="bg-blue-500 text-white py-2 px-4 cursor-not-allowed rounded-md">
                                        Like
                                    </a>
                                <% } %>
                            </div>
                        </div>
                    <% }) %>
                <% } else { %>
                    <h1 class="text-zinc-600">No Posts Yet.</h1>
                <% } %>
            </div>
        </div>
    </div>
</body>
</html>
