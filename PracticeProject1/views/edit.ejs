<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit File</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body>
    <div class="main w-full min-h-screen bg-zinc-900 p-10 text-white">
        <a href="/" class="text-blue-500 inline-block mb-10">GO Back to Home</a>

        <form action="/edit/<%= fileName %>" method="post">
            <% let title = fileName.replace(/\.[^/.]+$/, "").replace(/([A-Z])/g, ' $1').trim() %>
            <input class="w-1/4 px-5 py-4 block bg-zinc-800 rounded-md outline-none text-zinc-400" type="text" placeholder="Previous Name" name="previous" value="<%= title %>" readonly>
            <input class="w-1/4 px-5 py-4 block bg-zinc-800 rounded-md outline-none mt-2" type="text" placeholder="New Name" name="new">

            <textarea class="w-full px-5 py-4 block bg-zinc-800 resize-none mt-2 rounded-md outline-none" placeholder="Your Task Description goes here...." name="details"><%= fileData %></textarea>
            <button type="submit" class="bg-yellow-600 py-3 px-10 text-white mt-2 rounded-md">Edit</button>
        </form>
    </div>
</body>
</html>
